let age=12;
if(age>12)
    console.log("You are eligible for roller coaster ride");
//in any given case the condition inside if() statement fails we use else to notify user
else
    console.log("Not eligible")